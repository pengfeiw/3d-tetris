import React from "react";
import {Card} from "@material-ui/core";
import "./index.less";

const GameStatus = () => {
    return (
        <Card className="gameStatus"/>
    );
};

export default GameStatus;
